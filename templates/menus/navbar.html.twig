<nav class="navbar">
    <!-- Add your other nav elements here -->

    <div class="burger-menu">
        <i id="burger-icon" class="fas fa-bars" style="color:#ECDBBA;"></i>
    </div>
    <div class="navTitle"><a href="{{ path('app_home') }}">LeGuideDeBogdan &lt;/&gt;</a>
    </div>
    <div class="user-menu">
        <div class="user-icon-container">
            <i class="fas fa-user"></i>
            <div class="user-icon-background">
                <img src="/assets/icons/puce.svg">
            </div>
        </div>
        <div class="user-menu-dropdown">
            {% if app.user %}
            <a href="{{ path('app_profile_edit') }}">Votre Profil</a>
            {# <a href="{{ path('app_category_index') }}">Categories</a> #}
            <a href="{{ path('app_logout') }}">Déconnexion</a>
            {% else %}
            <a href="{{ path('app_login_route') }}">Connexion</a>
            <a href="{{ path('app_register') }}">Inscription</a>
            {% endif %}
        </div>
    </div>
</nav>