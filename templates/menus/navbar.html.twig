<nav class="navbar">
    <!-- Add your other nav elements here -->

    <div class="burger-menu">
        <i id="burger-icon" class="fas fa-bars"></i>
    </div>
    <div class="user-menu">
        <i class="fas fa-user"></i>
        <div class="user-menu-dropdown">
            {% if app.user %}
            <a href="{{ path('app_profile_show') }}">Profile</a>
            {# <a href="{{ path('app_category_index') }}">Categories</a> #}
            <a href="{{ path('app_logout') }}">Logout</a>
            {% else %}
            <a href="{{ path('app_login_route') }}">Login</a>
            <a href="{{ path('app_register') }}">Register</a>
            {% endif %}
        </div>
    </div>
</nav>